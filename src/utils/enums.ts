export enum ApiMethods {
  GET = 'GET',
  DELETE = 'DELETE',
  OPTIONS = 'OPTIONS',
  PATCH = 'PATCH',
  POST = 'POST',
  PUT = 'PUT',
}

export enum ApiEndpoints {
  USERS = 'users',
}

export enum SearchParamsKeys {
  SEARCH = 'q',
}

export enum AppRoutes {
  HOME = '/',
  ABOUT = '/about',
  USERS = '/users',
}
